# PRD — learn-agent-design

> 文档先行。本 PRD 是 features.json 的输入源。信息来自用户三项已确认决策 + 本地材料盘点，已填实。

## 背景与动机
用户系统学习「模型策略 PM（Agent 方向）」，在本地 Obsidian wiki 沉淀了一套别人没有的 Agent 知识体系：L3 PM 五维能力（任务路径/失败节点/错误恢复/透明度/边界行为）+ Agent 技术地基五层 + Harness/Gateway/上下文工程/评测/框架选型等约 75 个概念页。这些材料目前是「私人笔记」形态，价值高但不可分享、不成体系对外。

参照 walkinglabs 的 `learn-harness-engineering`（问题驱动讲义体课程站），把这套笔记**重写**成一门公开课，作为 Build in Public 作品，配合公众号「橙研所」与 3 个月成长计划的 Authority 阶段——把"学过"变成"能教"，沉淀个人影响力。

与 walkinglabs 的关系：它讲 **harness 工程**（怎么让 agent 可靠跑长任务）；本课讲 **Agent 产品/能力设计 + 工程地基**（怎么设计、评估一个 Agent），互补不重复。

## 目标用户
1. **主**：想转型/进阶到 Agent 方向的产品经理、模型策略 PM（求职者、在职 PM）。
2. **次**：做 Agent 应用的工程师、独立开发者，想要一套"产品视角 + 工程地基"的体系化认知。
3. **隐含**：用户自己——把知识从"记忆"固化为"可复用资产"，并作为面试/分享素材。

## 功能需求
（拆到可变成 features.json 原子 feature 的粒度）

### FR1 课程内容（双模块）
- **Module A「Agent 产品 / 能力设计」**：以 L3 五维为主轴 + 技术地基五层做底座。
- **Module B「Agent 工程地基」**：Harness 工程 + Gateway 工程 + 上下文工程七维 + 评测体系 + 框架选型。
- 每讲 = 一个 markdown 文件，问题驱动讲义体（立靶→框架→数据/case→做法→收口）。

### FR2 静态站
- 纯静态 HTML 多页，轻构建脚本把 `content/*.md` 渲染成 `site/*.html`。
- 页面类型：首页（课程总览 + 双模块卡片）、模块页（讲次列表）、讲义页（正文）。
- 顶部导航在所有页可达；讲义页有上一讲/下一讲。

### FR3 视觉
- 橙研所橙皮书风：橙色品牌渐变 + 卡片排版；文字为绝对主体；禁 emoji；图标 line-SVG；配图克制（每讲 ≤ 3-5 张）。
- 响应式：手机端可读。

### FR4 可部署
- build 产物 `site/` 可直接推 GitHub Pages（相对路径、无服务端依赖）。

## 技术约束
- 构建脚本优先 Python 3 标准库，尽量零三方依赖；若必须用 markdown 解析库，记入 requirements.txt 并说明。
- 零运行时依赖（产物是纯静态 HTML/CSS）。
- 内容源是用户私人 vault，**重写时脱敏**：不泄露未公开的项目代号/客户名/内部数据（妙招、法智、ECS 等内部细节按需抽象成通用案例）。

## 验收标准
- AC1：`python3 scripts/build.py` 无报错，产出 `site/index.html` + ≥1 篇讲义 HTML。
- AC2：浏览器打开首页，能看到橙皮书风格 + Module A / Module B 双卡片，点击可达讲义。
- AC3：讲义页正文按讲义体结构渲染，含至少一个框架表格。
- AC4：全站无 emoji（`grep` 可验）。
- AC5（M1 之后）：每新增一讲，build 仍通过且导航自动更新。

## 里程碑
- **M1 / S1（本次）**：站点骨架 + 橙皮书视觉 + build 脚本 + 第 1 讲样板，端到端跑通。**不一次堆完所有讲**。
- M1 / S2：Module A 讲义填充。
- M1 / S3：Module B 讲义填充。
- M1 / S4：部署 GitHub Pages + 首页打磨 + 资料库/技能区。
